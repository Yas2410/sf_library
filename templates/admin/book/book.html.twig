{% extends 'admin/common/base.html.twig' %}
{% block content %}

    <section>
    <div>
        <h1>{{ book.title }}</h1>
        <span> <strong>{{ book.author.firstName }} {{ book.author.lastName }}</strong></span>
        <span> <em>{{ book.nbPages }} pages</em></span>
        <p>{{ book.resume }}</p>
    </div>
    </section>
    

{% endblock %}